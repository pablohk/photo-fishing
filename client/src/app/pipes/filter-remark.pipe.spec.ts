import { FilterRemarkPipe } from './filter-remark.pipe';

describe('FilterRemarkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRemarkPipe();
    expect(pipe).toBeTruthy();
  });
});
